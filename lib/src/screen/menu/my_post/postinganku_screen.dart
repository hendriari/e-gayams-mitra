import 'package:flutter/material.dart';

class PostingankkuScreen extends StatelessWidget {
  const PostingankkuScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('postinganku'),
      ),
    );
  }
}
